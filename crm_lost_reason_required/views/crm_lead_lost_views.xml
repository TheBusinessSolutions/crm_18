<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- 
      No XML inheritance is needed. 
      The 'required=True' attribute in the Python model (crm_lead.py) 
      automatically makes the field required in the UI wizard.
    -->
</odoo>